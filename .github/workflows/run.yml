name: FINAL DEBUG

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: SHOW FILES
        run: ls -R

      - name: SHOW BOT FILE
        run: cat bot.py

      - name: PYTHON VERSION
        run: python --version

      - name: RUN BOT
        run: python -u bot.py
