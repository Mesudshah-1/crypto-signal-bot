name: debug

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: SHOW FILES
        run: ls -R

      - name: SHOW BOT
        run: cat bot.py

      - name: RUN PYTHON
        run: python -u bot.py
