name: Crypto Signal Bot

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt

      - run: python bot.py
        env:
          TELEGRAM_BOT_TOKEN: 8703847181:AAG7ZoIJ4XHpqniqm2wp16ZUCHJL7tIzctg
          GENERAL_CHANNEL_ID: -1003953455562
          VIP_CHANNEL_ID: -1003950012200
